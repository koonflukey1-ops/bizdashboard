export type Role='ADMIN'|'STAFF';
export type User={id:string;name:string;username:string;password:string;role:Role;active:boolean};
export type Product={id:string;code:string;name:string;sellingPrice:number;costPrice:number;stockQuantity:number;active:boolean;createdAt:string;updatedAt:string;deletedAt?:string};
export type Promotion={id:string;productId:string;minimumQuantity:number;promoUnitPrice:number;active:boolean;allowRepeatedSets:boolean;maximumUses?:number;startDate?:string;endDate?:string};
export type OrderItem={productId:string;productCodeSnapshot:string;productNameSnapshot:string;unitSellingPriceSnapshot:number;unitCostSnapshot:number;promotionSnapshot?:Promotion;quantity:number;lineNormalTotal:number;lineDiscount:number;lineFinalTotal:number};
export type Order={id:string;orderNumber:string;clientRequestId:string;customerName:string;customerPhone:string;customerAddress:string;status:'CONFIRMED'|'COMPLETED'|'CANCELLED';paymentStatus:'UNPAID'|'PAID'|'COD'|'PARTIALLY_PAID'|'REFUNDED';shippingStatus:'PENDING'|'PACKING'|'SHIPPED'|'DELIVERED'|'RETURNED'|'CANCELLED';items:OrderItem[];normalSubtotal:number;discountTotal:number;finalRevenueTotal:number;costTotal:number;profitTotal:number;createdBy:string;createdAt:string;cancelledBy?:string;cancelledAt?:string;cancellationReason?:string;stockRestored?:boolean};
export type Movement={id:string;productId:string;productCode:string;type:string;before:number;change:number;after:number;referenceType:string;referenceId:string;reason:string;createdBy:string;createdAt:string;requestId:string};
export type Audit={id:string;timestamp:string;user:string;action:string;entityType:string;entityId:string;before?:unknown;after?:unknown;requestId:string;userAgent:string};
export type LineJob={id:string;orderId:string;state:'PENDING'|'PROCESSING'|'RETRY'|'SENT'|'FAILED';attempts:number;nextAttemptAt?:string};
export type Settings={lineEnabled:boolean;lineToken:string;lineTarget:string;lineSecret:string;n8nUrl:string;apiBaseUrl:string;apiKeyLabel:string;dailyTime:string};
export type DB={users:User[];products:Product[];promotions:Promotion[];orders:Order[];movements:Movement[];audits:Audit[];lineJobs:LineJob[];settings:Settings;lastBackupAt?:string};
